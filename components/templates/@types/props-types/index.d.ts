// Type definitions for prop-types of each template components

export type NavbarProps = {
    name: string
    currPath: `/${string}`
}

export type NavItem = {
    name: string
    path: `/${string}`
}
